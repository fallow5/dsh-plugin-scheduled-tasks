# The @opendsh/dsh-plugin-session-search bundle patch: mounts the plugin's
# server half as one Cordis loader row. Apply this bundle after `dsh-base` +
# `dsh-web-app`, which provide `storageDomain` and `webServer`.

- insert:
    - id: session-search
      name: '@opendsh/dsh-plugin-session-search'
